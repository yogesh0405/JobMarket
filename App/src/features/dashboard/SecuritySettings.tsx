import React from 'react';
import { SecuritySettings as ProfileSecuritySettings } from '../../components/profile/SecuritySettings';

export const SecuritySettings: React.FC = () => {
  return <ProfileSecuritySettings />;
};
